"""Offline proof of concept for evalanche.

Generated by:

    docker compose up -d postgres
    uv run python scripts/run_poc.py

Artifacts prove generate → score → report without Ollama/GPU.
Asserted by tests/test_poc.py in CI.
"""
